# System files
.DS_Store
Thumbs.db

# IDEs
.vscode/
.idea/

# Python build artifacts
__pycache__/
*.py[cod]
.pytest_cache/

# Large files / PCAPs (Optional)
# If your PCAPs get > 50MB, uncomment the line below and use Git LFS
# *.pcap

# Temporary / Log files
*.log
*.tmp
# The forensic audit log is evidence, not a log file to discard: it is committed
# on purpose, so it is un-ignored here.
!automation/ingest_audit.log
